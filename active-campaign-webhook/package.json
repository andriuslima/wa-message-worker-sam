{
  "name": "active-campaign-webhook",
  "version": "1.0.0",
  "description": "Webhook to process post message from Active Campaign and send to queue",
  "main": "index.js",
  "author": "Andrius Lima",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "sucrase . -d ../bin/active-campaign-webhook --transforms typescript,imports && cp package.json ../bin/active-campaign-webhook/",
    "lint": "eslint . --ext .ts",
    "build": "tsc && cp package.json ../bin/active-campaign-webhook/"
  }
}
