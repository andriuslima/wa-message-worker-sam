{
  "name": "wa-sender",
  "version": "1.0.0",
  "description": "Serveless application to process phones and send WhatsApp messages",
  "main": "index.js",
  "author": "Andrius Lima",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "sucrase . -d ../bin/sender --transforms typescript,imports && cp package.json ../bin/sender/",
    "lint": "eslint . --ext .ts",
    "build": "tsc && cp package.json ../bin/sender/"
  },
  "dependencies": {
    "axios": "^0.21.2"
  }
}
