{
  "name": "wa-sender-retry",
  "version": "1.0.0",
  "description": "Serverless application retrieve message from DLQ to reprocess them",
  "main": "index.js",
  "author": "Andrius Lima",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "sucrase . -d ../bin/sender-retry --transforms typescript,imports && cp package.json ../bin/sender-retry/",
    "lint": "eslint . --ext .ts",
    "build": "tsc && cp package.json ../bin/sender-retry/"
  }
}
