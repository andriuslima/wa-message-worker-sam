{
  "name": "parser",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Andrius Lima",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "sucrase . -d ../bin/parser --transforms typescript,imports && cp package.json ../bin/parser/",
    "lint": "eslint . --ext .ts",
    "build": "tsc && cp package.json ../bin/parser/"
  }
}
